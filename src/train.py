"""Training script - not used for inference-only CISC experiment."""

# This file is not used in the current experiment.
# The CISC experiment is inference-only and does not require training.
# All logic is in src/main.py which calls src/model.py for inference.

pass
